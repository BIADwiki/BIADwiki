#------------------------------------------------------------------
# Runs all R scripts beginning with 'make.'
#------------------------------------------------------------------
source('functions.R')
#------------------------------------------------------------------
files <- list.files()
files_to_run <- files[substr(files,1,5)=='make.']
for(file in files){
	print(date())
	source(file)
	}
date()
#------------------------------------------------------------------
	
	
	
	